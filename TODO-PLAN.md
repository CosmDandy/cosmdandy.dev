# Разбор todo_new.md

Источник — `todo_new.md`, надиктованный текст. Отметка `[x]` ставится только
после проверки: кадр, `behave.mjs`, `audit_text.py`, `todo_check.py` или замер в
браузере — что уместно.

Прошлый круг правок закрыт: сто тридцать три пункта держат проверки в
`tools/todo_check.py`, по одной на пункт. Что из прошлого круга так и не сошлось
и почему — в конце файла.

## S. Режим лупы

- [x] S1. Приближение резкое и дёргается — вести его плавно, а не ступенькой.
- [x] S2. Приближает в левый верхний угол, а не в точку под курсором.
- [x] S3. Схему можно двигать вправо бесконечно — справа нужен край, как слева и по вертикали.

## T. Выделение и фокус

- [x] T1. Кнопка снятия крышки обводится бело-синей рамкой браузера — убрать её со всех органов управления.

## U. Блоки питания

- [x] U1. Вынимание слишком медленное. Вставление оставить как есть — оно устраивает.

## V. Рейзеры

- [x] V1. Вынимается не по диагонали, а в два движения: сначала строго вверх на высоту контакта, потом вправо.
- [x] V2. Подъём: восемь десятых пути туго, остаток быстро.
- [x] V3. Ход быстрее, жёстче и увереннее прежнего.
- [x] V4. Перекрытие блока питания вынутым рейзером — допустимо, это перспектива.
- [x] V5. Вентиляционные отверстия на рейзерах не просвечивают.

## W. Процессор

- [x] W1. Анимация «ездит влево-вправо» — закрепить движение на самом процессоре.
- [x] W2. Надпись INSTALL всё ещё крупная — уменьшить.
- [x] W3. Крышка уходит вправо-вверх так, чтобы её нижняя кромка легла на центр посадочного места.
- [x] W4. Процессор откидывается влево-вниз по той же диагонали.

## X. Память

- [?] X1. Шлейфы на плашке — по середине. **Не понял, о чём речь.** На плашке
  памяти есть наклейка со штрих-кодом, серебристый пробор у контактов и ряд
  чипов. Что из этого «шлейфы» и относительно чего «посередине» — по вертикали
  плашки или по её длине?
- [x] X2. Штрих-код считать от хэша коммита, а не от произвольного текста.
- [x] X3. Чипы вразбежку по высоте: горизонтальный у верхней кромки, вертикальный на два миллиметра ниже.

## Y. Шелкография и подписи

- [x] Y1. Подписи просвечивают — под `CPU0 · SP5` видны дорожки.
- [x] Y2. Под `CPU0` видны медные пятачки.
- [x] Y3. `DIMM_CPU0_A0` стоит вне платы.
- [?] Y4. Медные пятачки над `CPU0` выходят за кромку текстолита. **Замерил —
  не нашёл.** За верхней кромкой ровно один кружок, и тот в левом углу платы,
  далеко от процессора. За нижней сорок один, но это крепёжные винты шасси, они
  и должны быть снаружи. Покажите кадром, о каких именно идёт речь.
- [x] Y5. Индикатор heartbeat налезает на надпись BMC — сдвинуть правее.
- [?] Y6. `cosmdandy · service · mail` сдвинуть чуть левее. **Не нашёл такой
  строки.** На плате есть шелкография `COSMDANDY` по центру нижнего поля и
  бирки-ссылки справа. Речь про них?

## Z. Диагностика и сборка

- [x] Z1. Ошибка после вынутого узла держится до RESET. Сделать так, чтобы жёлтая лампа горела, пока не запросят журнал ошибок: сервер узнаёт об исправлении, когда его об этом спрашивают.
- [x] Z2. На сборке между блоками питания с вентиляторами и памятью с процессором простой — убрать паузу.

## AC. Четвёртая диктовка — сделано

- [x] AC1. На сборке блоки питания, вентиляторы и райзеры возили с собой лампу гнезда: правило висело на всей группе, а лампа нарисована на плате, рядом с телом узла. Теперь едет только модуль.
- [x] AC2. Кнопка сервиса поставлена по числам: x=825, y=570 при ширине 114. Рамка блока расширена влево — место у детали назначено снаружи, а рамка лишь описывает занятое.
- [x] AC3. Соты на кронштейнах райзеров стали настоящими дырками: лист пробивается маской. Полупрозрачной заливки было мало — под ней оставался непрозрачный кронштейн.
- [x] AC4. Соты на заднем борту — тоже маской, сквозные.
- [x] AC5. Диски вынимаются полностью, лампы вынутого каддика гаснут.
- [x] AC6. Светлая тема снята со схемы: цвета машины подобраны под тёмный фон, а перекраска шла рывком — переменная не интерполируется, и машина перещёлкивалась посреди плавного перехода страницы. Тема осталась у страницы вокруг.
- [x] AC7. Указатели ключа на процессоре и на сокете убраны.
- [x] AC8. Поле контактов уменьшено и убрано под подложку: его прямой угол торчал из-под срезанного угла текстолита. Срез уменьшен с 18 до 10 и оставлен только на подложке — на металле крышки его больше нет.
- [x] AC9. Семисегментник показывает код готовности и на повторном заходе: он заводился только в powerOn(), а питание восстанавливается из сохранённого состояния мимо неё.
- [x] AC10. Кнопки лупы и пересборки меняют размер и место переходом, а не скачком.
- [x] AC11. Плата продлена до борта: между её прежней кромкой и стальной стенкой оставалось 76 единиц пустого серого поля.

## AJ. Девятая диктовка

- [x] AJ1. Кнопка пересборки выплывает вместе с остальными, а не висит на месте с первого кадра: `contentEnter` с задержкой 740 мс — третья в ряду после темы (620) и лупы (680). Состояние «занята» переехало с `opacity` на `filter: opacity()`: сборка идёт с первой секунды, и правило гасило бы кнопку ещё до её появления, споря с анимацией входа за одно свойство.
- [x] AJ2. Имя на схеме тоже выплывает. Правило появления написано по тегу `h1` — на карточке имя им и набрано, а на схеме это `h2`, и оно под правило не попадало: должность и строка о работе выплывали, имя стояло.

## AI. Восьмая диктовка

- [x] AI1. Площадка сокета — по размеру текстолита подложки, и срез у неё тот же и там же: процессор ложится на неё всей подложкой, а не крышкой.
- [x] AI2. Поле ножек — по кристаллу: контакт идёт там, где над ним крышка, и ряды за её габарит не выходят.
- [x] AI3. Ножки крупнее: шаг 2.6 → 2.9, штрих 0.5 → 0.7 при толщине 1.5.
- [x] AI4. INSTALL без подложки и по середине полосы между площадкой и кромкой гнезда.
- [x] AI5. Кнопка пересборки гаснет переходом, а не скачком.
- [x] AI6. В режиме лупы вернулась точечная сетка фона: глухая заливка поля накрывала её собой.
- [~] AI7. Наклон машины пробовали поднять до 18°, вернули 13°: разница с плоской лупой читалась сильнее, но сама схема — хуже, дальний край мельчает быстрее, чем растёт выразительность наклона.
- [x] AI8. Бирки после выхода из лупы возвращаются за ~0.9 с вместо ~2: правило крышки держало полторы секунды выдержки, рассчитанной на едущий лист. Когда машина просто собралась обратно, ждать нечего.

## AH. Седьмая диктовка

- [x] AH1. Крышка процессора уменьшена под поле контактов: `IHS_INSET = 11` на сторону. Кристалл под теплораспределителем всегда меньше площадки с ножками — иначе он свисал бы с неё. Текстолит подложки прежнего размера.
- [x] AH2. Посадочная площадка со срезанным углом, как и подложка над ней: край у них общий, и прямоугольная площадка под срезанной подложкой читалась двумя разными деталями.
- [x] AH3. Номера болтов набиты до крышки, а не после: снятый процессор уезжает влево-вниз ровно на третий номер, и цифра оказывалась поверх детали в руке.
- [x] AH4. Тугой ход блоков питания и райзеров короче в полтора раза: `--drag` 0.72 → 0.48 с, `--riser-lift` 0.5 → 0.33 с.
- [x] AH5. Диск въезжает мягче: `--press-seat` 0.9 → 1.2 с. Плюс поломка — обратный ход шёл кривой ручки в 0.13 с, потому что состояние «каддик в корзине, защёлка откинута» неотличимо по классам от «защёлку откинули и потянули». Обратный ход помечает себя классом `back`.
- [x] AH6. Лампы каддика гаснут на первом же движении, не дожидаясь полного вылета: контакт теряется в первом сантиметре.

## AG. Шестая диктовка

- [x] AG1. Тонкой серой полоски между бортом и платами разъёмов нет: задняя панель одна, лист начинается левее стойки кронштейна.
- [x] AG2. Перфорация шире и смещена влево — идёт по всему листу, а не по просветам между окнами.
- [x] AG3. Панель кладётся первым слоем: она подложка, на которой стоит всё остальное. Пока добавлялась последней, лист ложился поверх гнёзд.
- [x] AG4. От нижнего райзера поле сот начинается с отступом: у самой кромки проёма дырки читались обгрызенным краем.
- [x] AG5. Просвет между двумя райзерами оставлен как есть.
- [x] AG6. Вырезы крышки согласованы с проёмами панели и кронштейнами: и лист, и панель считают их из одних имён — RISER, IO_Y с JACK_PITCH. Проверка B12.
- [x] AG7. Свечение за машиной растянуто за её габарит и размыто сильнее: видны оба края градиента, а не один центр. Яркость та же, прибавлена площадь.

## AF. Консоль

- [x] AF1. Фокус возвращается в строку ввода после любого действия на схеме: щёлкнул по узлу — и сразу можно набирать команду.

## AE. Пятая диктовка — сделано

- [x] AE1. У нижнего райзера пропадала часть детали: белое поле маски накрывало только перфорированную полосу, а всё, что за маской, исчезает.
- [x] AE2. Скачок вправо при вынимании райзера: отвод шёл кривой с максимальной скоростью в начале, и после паузы это читалось скачком. Мягкий старт.
- [x] AE3. Платы разъёмов уехали вместе с кромкой текстолита: они считались от `X_PCB_END`, а он ушёл к борту. Заведён `X_IO_END` — их собственное место.
- [x] AE4. Каддик и накопитель снова разъезжались на вставке: у них были разные правила. Теперь одно на обоих.
- [x] AE5. По первому щелчку каддик уже выходит на сантиметр вслед за ручкой, а не стоит.
- [x] AE6. Поле контактов было мельче корпуса процессора — ножек меньше, чем сам корпус, не бывает. Отступ два вместо семи.

## AD. Четвёртая и пятая диктовки — сделано

- [x] AD1. Задняя панель: тонкие заполнители убраны, подложка одна и пробита насквозь.
- [x] AD2. Надпись `2× 10G SFP+` перенесена с торца карты на плату.
- [x] AD3. Обводки блоков на крышке кончаются у кромки перфорации, а не идут поверх дырок.
- [x] AD4. Обводки райзеров и платы разъёмов — одной ширины и от одного левого края.
- [x] AD5. Два выреза вместо одного: под оба слота райзеров разом и под гнёзда сетевых карт. Под USB выреза нет.
- [x] AD6. Спуски вырезов под 45° трапецией, все углы скруглены — и внешние, и внутренние.
- [x] AD7. Задняя панель одной широкой планкой от стойки кронштейна до внешней кромки борта, с проёмами под райзеры.

## AB. Третья диктовка

- [x] AB1. Градиент на процессоре ездил и мельчал — дубль правила с масштабом стоял ниже основного и молча перебивал его.
- [x] AB2. Ход блика укорочен: был почти во всю крышку и читался ездящим прямоугольником.
- [x] AB3. Полоса блика накрывает крышку целиком, а «блик, а не заливка» держится прозрачной каймой на трети длины с каждой стороны.
- [x] AB4. Обозначение `CPU0` набито под деталью, а не поверх неё.
- [x] AB5. Отвод райзера вбок ждёт конца подъёма: замерено, трогался на 0.30 с при подъёме в 0.5 с — конец подъёма шёл наискось.
- [x] AB6. Кнопки лупы и пересборки в режиме лупы разведены: обе прижаты к правому краю с зазором.
- [x] AB7. Браузерных рамок фокуса внутри машины нет вовсе — перечислять органы поимённо было мало, таб ходит и по биркам, и по группам узлов.
- [x] AB8. Переключатель сервиса поднят на y=548 из-под бирок Twitter и Email.
- [x] AB9. Серебристые контакты на проборе плашки убраны.
- [?] AB10. «Пройдись по слоям, раздели их» — поправлено то, на что показали: подпись процессора. Полный пересмотр порядка слоёв не делал: он трогает все двадцать блоков сразу, и без конкретных мест это перебор наугад. Покажите, что ещё видно не так.

## AA. Плата, блоки питания и шапка (вторая диктовка)

- [?] AA1. Плату растянуть на всю ширину корпуса. **Нужен один ответ.** По
  горизонтали растягивать некуда: плата идёт от стенки вентиляторов (x=346) до
  x=1206, а дальше задняя панель с разъёмами — ей эти сто восемь единиц и
  нужны. Значит речь про вертикаль: сейчас текстолит занимает y 18…845 при
  корпусе 863. Растянуть до самых кромок, оставив только поле под винты?
- [?] AA2. Блоки питания развести. **Тот же вопрос.** Верхний стоит на y=22 —
  выше только кромка корпуса, двигать некуда. Нижний на 696, до низа 22
  единицы. То есть развести можно ровно на эти двадцать с небольшим — этого
  хватит, или надо менять высоту самих блоков?
- [x] AA3. Рейзеры не двигал.
- [x] AA4. В режиме лупы убрать кнопку темы, а кнопки перезагрузки и лупы сделать крупнее — по высоте как имя с должностью.
- [x] AA5. Кнопку лупы в этом режиме подсветить красным как включённую.

---

## Из прошлого круга: не сошлось

- **F7. Рейзер вверх поднять некуда.** Блок питания 1 занимает y 18…155, вынутый
  рейзер 1 начинается на y=154, зазора нет. Пункт V4 это снимает: перекрытие
  признано допустимым.
- **D12. Разлёт крышки и процессора.** Замерено: при радиаторе на 252 вправо он
  ложится на первый вентилятор, при процессоре на 152 влево — на второй, пятый и
  шестой. Пункты W3 и W4 задают направление точнее, и упор надо перемерить под
  них.
- **H6б. Рамка воздухозаборника справа от корзины** — формулировка не разобрана.
  Фронт нарисован единым листом буквой Г, и перфорация справа от корзины на нём
  уже есть (`front_panel.py:59`, полоса x 88…158).

## Из прошлого круга: сознательные отступления

- **Обратный ход — не разворот во времени, а посадочная кривая узла.** Разворот
  переносит сопротивление в конец хода: по числам зеркало, на глаз — рывок с
  места. Каддик на развёрнутой кривой застревал на девяноста двух процентах пути.
- **C2.** Пара вентиляторов у крайних воздуховодов сохранена, порядок переставлен:
  на схеме `FAN1` вверху, `FAN8` внизу.
- **C4.** На отливке `CPU 0` / `CPU 1`, а не `CPU 1` / `CPU 2` — плата зовёт их
  нулём и единицей.
- **R1.** Ревизии из артефактов Claude Web не сделать: у артефакта нет ни
  коммита, ни места в репозитории.
